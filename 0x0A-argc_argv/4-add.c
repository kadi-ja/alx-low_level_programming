#include <stdio.h>
#include <stdlib.h>

/**
 *main - a program that adds positive numbers.
 *@argc: argument count
 *@argv: argument verctor
 *Return: always 0
 */

int main(int argc, char *argv[])
{
  int i;
  for (i = 1; i < argc; i++);
  {
  if (argc < 2)
    {
      printf("0\n");
     }
  if else (is digit(argv[i]))
	    
	      printf("Error\n");
	      return(1);
	      
  }
}
