#include "main.h"
/**
 *_abs - computes the absolute value of an integer.
 *
 *Return: always 0
 */
int _abs(int r)
{
resut = r * -1;
if (r > 0)
{
_putchar(r);
}
else if (r == 0)
{
_putchar(r);
}
else
{
result (result);
}
return (0);
}
