#include "main.h"
/**
 *_isalpha -  checks for alphabetic character.
 *@c: char type letter
 *Return: always 0
 */
int _isalpha(int c)
{
  if ((c >= 96 && c <= 123) || (c >= 65 && c <= 90))
{
return (1);
}
else
{
return (0);
}
